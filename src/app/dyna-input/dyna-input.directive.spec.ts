import { DynaInputDirective } from './dyna-input.directive';

describe('DynaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DynaInputDirective();
    expect(directive).toBeTruthy();
  });
});
