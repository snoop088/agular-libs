import { InputViewDirective } from './input-view.directive';

describe('InputViewDirective', () => {
  it('should create an instance', () => {
    const directive = new InputViewDirective();
    expect(directive).toBeTruthy();
  });
});
