import { InputEditDirective } from './input-edit.directive';

describe('InputEditDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEditDirective();
    expect(directive).toBeTruthy();
  });
});
