import { SeSubmenuDirective } from './se-submenu.directive';

describe('SeSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SeSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
