import { SeSubmItemDirective } from './se-subm-item.directive';

describe('SeSubmItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SeSubmItemDirective();
    expect(directive).toBeTruthy();
  });
});
