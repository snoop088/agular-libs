import { SeSubmenuItemDirective } from './se-submenu-item.directive';

describe('SeSubmenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SeSubmenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
